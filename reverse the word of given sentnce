#include<bits/stdc++.h>
using namespace std;


int main(){
   string str,str1=" ";
  stack<string>stac;
  getline(cin, str);
  for(int i=0;i<str.size();i++){
    if(isspace(str[i]) || i==str.size()-1){
        stac.push(str1);
        str1=" ";
    }
    else{
      str1+=str[i];
    }
  }
  while(!stac.empty()){
    cout<<stac.top();
    stac.pop();
  }
  return 0;

}
